Number {
    String value;
    print value;
    validator Javascript {{
        const num = @@value;
        if (isNaN(parseFloat(num)) || !isFinite(num)) {
            return `"${num}" is not a number.`;
        }
        return true;
    }}
}