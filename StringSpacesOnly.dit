StringSpacesOnly {
    String value;
    print value;
    validator Javascript {{
        let str = @@value;
        if (/[\t\r\n\f]/.test(str)) {
            return `The following string contains whitespace other than spaces:
            "${str}"`;
        }
        return true;
    }}
}